# Copy to .env and adjust. All keys optional — defaults give SQLite + DEBUG=True.

SECRET_KEY=change-me-in-production
DEBUG=True

# --- Database (default: SQLite at BASE_DIR/db.sqlite3) ---
# Switch to PostgreSQL by uncommenting and filling the values below:
# DB_ENGINE=django.db.backends.postgresql
# DB_NAME=tcc_webapp
# DB_USER=
# DB_PASSWORD=
# DB_HOST=localhost
# DB_PORT=5432
