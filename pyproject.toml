[project]
name = "tcc-webapp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[tool.ruff]
line-length = 100
target-version = "py313"

[tool.ruff.lint]
# correctness + import hygiene first; style rules join after the refactor
select = ["E4", "E7", "E9", "F", "I"]
